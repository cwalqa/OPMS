APP_NAME="ColorWrap Inc"
APP_ENV=production
APP_KEY=base64:WeRwS8DmlnmxMHUU+QhjzoaFCWAM7K76M45UpzBCu2E=
APP_DEBUG=false
APP_URL=https://yourproductiondomain.com

LOG_CHANNEL=stack
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=error

DB_CONNECTION=mysql
DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=techtyqy_cwiquickbooks
DB_USERNAME=root
DB_PASSWORD=

BROADCAST_DRIVER=log
CACHE_DRIVER=file
FILESYSTEM_DRIVER=local
QUEUE_CONNECTION=sync
SESSION_DRIVER=file
SESSION_LIFETIME=120

MEMCACHED_HOST=127.0.0.1

REDIS_HOST=127.0.0.1
REDIS_PASSWORD=
REDIS_PORT=6379

MAIL_DRIVER=smtp
MAIL_HOST=smtp.gmail.com
MAIL_PORT=587
MAIL_USERNAME=datapluzzdeveloper@gmail.com
MAIL_PASSWORD="${MAIL_APP_PASSWORD}"
MAIL_ENCRYPTION=tls
MAIL_FROM_ADDRESS=datapluzzdeveloper@gmail.com
MAIL_FROM_NAME="${APP_NAME}"

AWS_ACCESS_KEY_ID="${AWS_ACCESS_KEY}"
AWS_SECRET_ACCESS_KEY="${AWS_SECRET_KEY}"
AWS_DEFAULT_REGION=us-east-1
AWS_BUCKET="${AWS_BUCKET_NAME}"
AWS_USE_PATH_STYLE_ENDPOINT=false

PUSHER_APP_ID="${PUSHER_ID}"
PUSHER_APP_KEY="${PUSHER_KEY}"
PUSHER_APP_SECRET="${PUSHER_SECRET}"
PUSHER_APP_CLUSTER=mt1

MIX_PUSHER_APP_KEY="${PUSHER_APP_KEY}"
MIX_PUSHER_APP_CLUSTER="${PUSHER_APP_CLUSTER}"

QUICKBOOKS_CLIENT_ID="${QB_CLIENT_ID}"
QUICKBOOKS_CLIENT_SECRET="${QB_CLIENT_SECRET}"
QUICKBOOKS_REDIRECT_URI=http://localhost:8000/quickbooks/callback
QUICKBOOKS_ENVIRONMENT=Production

# Security enhancements
FORCE_HTTPS=true
SESSION_SECURE_COOKIE=true
SESSION_COOKIE_HTTPONLY=true
SESSION_COOKIE_SAMESITE=lax

# Rate limiting
THROTTLE_MIDDLEWARE_ENABLED=true
THROTTLE_LIMIT=60
THROTTLE_DECAY_MINUTES=1